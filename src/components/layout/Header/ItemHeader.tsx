import type { IconType } from "react-icons";


interface ItemHeaderProps {
    nameItem: string;
    iconItem: string;
}

export const ItemHeader = ({nameItem, iconItem}: ItemHeaderProps) => {
	return (
		<li className="flex items-center ">
			
		</li>
	);
};
