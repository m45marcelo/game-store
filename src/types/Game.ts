export interface Game{
    id: string;
    name: string;
    section: string
    inOffer: boolean;
    price: number;
    oldPrice: number;
    totalDiscount: number
    cover: string;
    developer: string;
}